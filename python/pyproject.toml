[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "neuralos"
version = "0.2.3"
description = "Python bindings for the NeuralOS LLM inference engine"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = ["pytest"]
