/**
 * @file context.cpp
 * @brief Core scheduler context — owns the DAG and provider registry.
 *
 * Stub for Phase 1. Will be expanded with:
 *   - Global unified memory allocator
 *   - Provider routing table
 *   - Context Hub for multi-agent state sharing
 */

#include "neurofabric/abi/neuro_fabric_abi.h"

namespace nf {

struct nf_context_impl {
    // Phase 1 placeholder — will hold DAG root, allocator, provider map.
};

} // namespace nf
